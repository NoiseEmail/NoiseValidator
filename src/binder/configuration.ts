import { BinderConfiguration } from "./types";

export default {
    method: 'GET',
    middleware: {},
    schemas: {
        body: {},
        query: {},
        headers: {},
        output: void 0
    }
} as BinderConfiguration<any, any, any, any, any>;