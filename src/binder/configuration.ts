import { BinderConfiguration } from "./types.d";

export default {
    method: 'GET',
    middleware: {},
    schemas: {
        body: {},
        query: {},
        headers: {},
        output: void 0
    }
} as BinderConfiguration<any, any, any, any, any>;