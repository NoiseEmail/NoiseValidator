import { BinderConfiguration } from "./types";

export default {
    middleware: {},
    schemas: {
        body: {},
        query: {},
        headers: {}
    }
} as BinderConfiguration<any, any, any, any>;